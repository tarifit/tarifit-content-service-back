spring:
  application:
    name: tarifit-content-service-test
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: tarifittranslator_test

server:
  port: 0

logging:
  level:
    com.tarifit: DEBUG
    org.springframework.test: DEBUG
    org.springframework.web: DEBUG
